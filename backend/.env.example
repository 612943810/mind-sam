# Backend environment example
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/mind-sam?retryWrites=true&w=majority
# For old Atlas style vars (optional if MONGO_URI set):
# DATABASE_NAME=<your-db-name>
# DATABASE_PASSWORD=<your-db-password>

JWT_SECRET=replace_with_secure_random_string
PORT=3001
NODE_ENV=development
